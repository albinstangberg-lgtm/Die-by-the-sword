node_modules/

# Build output. `dist` is what `npm run build` writes; the glob also covers
# throwaway bundles (dist-probe and friends) used to check something in a
# browser without touching the committed source.
dist*/
*.local
.DS_Store
